<Window x:Class="coach_search.Views.UserProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Профиль пользователя" Height="400" Width="350"
        WindowStyle="None" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="#FFF7F2" BorderBrush="#E6C4A8" BorderThickness="2" CornerRadius="10" Padding="20">
        <StackPanel>
            <TextBlock Text="Профиль пользователя" Style="{StaticResource TitleText}" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            
            <!-- Фото -->
            <Ellipse Width="100" Height="100" HorizontalAlignment="Center" Margin="0,0,0,15" Stroke="#E6C4A8" StrokeThickness="3">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding PhotoPath}" Stretch="UniformToFill"/>
                </Ellipse.Fill>
            </Ellipse>
            
            <!-- Информация -->
            <TextBlock Text="{Binding FullName}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Email}" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Phone}" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <!-- Кнопка закрыть -->
            <Button Content="Закрыть" Width="120" Height="35" HorizontalAlignment="Center" 
                    Style="{StaticResource AccentButton}" Click="CloseButton_Click"/>
        </StackPanel>
    </Border>
</Window>

